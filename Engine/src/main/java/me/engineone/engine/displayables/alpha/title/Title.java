package me.engineone.engine.displayables.alpha.title;

public interface Title {
    Title setTitle(CharSequence text);

    Title setSubtitle(CharSequence text);

    Title setFooter(CharSequence text);
}